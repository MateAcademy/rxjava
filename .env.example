# Шаблон. Скопировать в .env и заполнить: cp .env.example .env

# PostgreSQL
DB_NAME=
DB_USER=
DB_PASSWORD=

# pgAdmin (пароль минимум 6 символов, иначе контейнер не стартует)
PGADMIN_EMAIL=
PGADMIN_PASSWORD=